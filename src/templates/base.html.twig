<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
<body>
<div class="container">
    {% if app.user %}
        <div class="connect">
            You are logged in as  <b>{{ nickname }}</b> | <a href="{{ path('app_logout') }}">Logout</a>
        </div>
    {% endif %}
    <div class="base-block">
        <img src="/logo-lehibou.png" width="100px;">
        <p class="lead text-center">
            Le Hibou's Chatroom
        </p>
    </div>
    {% block body %}{% endblock %}
</div>
</body>
</html>
